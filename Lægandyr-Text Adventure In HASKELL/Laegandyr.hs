import System.Random
import LaegandyrDice
import Text
import Support
import CharCreation
import GameLoop
    --note to self: all the same (for my purposes)
    --putStrLn $ show (read str :: Int)
    --putStrLn (show (stringToInt str))
    --print str
	
main :: IO ()
main = createChar